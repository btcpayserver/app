@attribute [Route(Routes.Index)]
@inherits SignedOutBasePage
@layout BaseLayout

@code {
    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();

        if (AuthState == null) return;
        await RedirectForAuthState(AuthState);
    }
}
